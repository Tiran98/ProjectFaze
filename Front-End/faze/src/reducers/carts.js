const reducer = (carts = [], action) => {

    // switch (action.type) {
    //     case 'FETCH':
    //         return action.payload;
    //     case 'CREATE':
    //         return [...carts, action.payload];
    //     default:
    //         return carts;
    // }
};

export default reducer;